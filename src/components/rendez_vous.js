import React from 'react';
import { StyleSheet, Text, View, ImageBackground } from 'react-native';

export default class rendez_vous extends React.Component {
  render() {
    return (
      <Text>
          test
      </Text>
    );
  }
}

const styles = StyleSheet.create({
    background: {
      width: '100%',
      height: '100%'
    }
});